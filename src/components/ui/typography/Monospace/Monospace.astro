---
import classNames from "../../../../utils/classNames";
import { monospaceClassName, type MonospaceProps } from "./styles";
import {
  sharedTypographyStyles,
  type SharedTypographyProps,
} from "../sharedTypographyProps";
type Props = MonospaceProps & SharedTypographyProps;
---

<span
  class={classNames(
    monospaceClassName(Astro.props),
    sharedTypographyStyles(Astro.props)
  )}><slot /></span
>
