---
import Heading from "../components/ui/typography/Heading.astro";
import Text from "../components/ui/typography/Text.astro";
import Monospace from "../components/ui/typography/Monospace.astro";
import Link from "../components/ui/typography/Link.astro";
import Button from "../components/ui/Button.astro";
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bitter:ital,wght@0,100..900;1,100..900&family=Fanwood+Text:ital@0;1&family=Schibsted+Grotesk:ital,wght@0,400..900;1,400..900&family=Sometype+Mono:ital,wght@0,400..700;1,400..700&display=swap"
      rel="stylesheet"
    />
    <meta name="generator" content={Astro.generator} />
    <title>
      {title}
    </title>
  </head>

  <body class="bg-beige-100 text-stone-800">
    <div class="container mx-auto p-12">
      <Heading level={1}>Design by nature.</Heading>
      <Heading level={2}>Design by nature.</Heading>
      <Heading level={3}>Design by nature.</Heading>
      <Heading level={4}>Design by nature.</Heading>
      <Heading level={5}>Design by nature.</Heading>
      <Heading level={6}>Design by nature.</Heading>
      <Monospace size="xs" className="uppercase"> I am monospaced </Monospace>
      <Text size="sm">
        Every detail of the AutoCamp experience combines the best of mid-century
        design with modern amenities: custom-designed Airstreams, gourmet food,
        guided experiences, and four-star service, all curated for you to
        reconnect with your surroundings.
      </Text>
      <Text size="xs">
        Every detail of the AutoCamp experience combines the best of mid-century
        design with modern amenities: custom-designed Airstreams, gourmet food,
        guided experiences, and four-star service, all curated for you to
        reconnect with your surroundings.
      </Text>
      <div class="flex items-center space-x-2">
        <Link variant="text" href="https://www.google.com">standard link</Link>
        <Link variant="mono" href="https://www.google.com">monospace link</Link>
      </div>
      <Heading className="mt-4" level={3}>Small buttons</Heading>
      <div class="grid grid-cols-3 gap-4 mt-4">
        <Button size="sm" variant="primary">primary button</Button>
        <Button size="sm" variant="outline">outline button</Button>
        <Button size="sm" variant="ghost">ghost button</Button>
        <Button size="sm" variant="danger">danger button</Button>
      </div>
      <Heading className="mt-4" level={3}>Medium buttons</Heading>
      <div class="grid grid-cols-3 gap-4 mt-4">
        <Button size="base" variant="primary">primary button</Button>
        <Button size="base" variant="outline">outline button</Button>
        <Button size="base" variant="ghost">ghost button</Button>
        <Button size="base" variant="danger">danger button</Button>
      </div>
      <Heading className="mt-4" level={3}>Large buttons</Heading>
      <div class="grid grid-cols-3 gap-4 mt-4">
        <Button size="lg" variant="primary">primary button</Button>
        <Button size="lg" variant="outline">outline button</Button>
        <Button size="lg" variant="ghost">ghost button</Button>
        <Button size="lg" variant="danger">danger button</Button>
      </div>
      <slot />
    </div>
  </body>
</html>
